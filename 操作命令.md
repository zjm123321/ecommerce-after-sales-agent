# 操作命令

## 项目

| 命令 | 作用 |
|---|---|
| `.\.venv\Scripts\Activate.ps1` | 激活项目虚拟环境 |
| `python -m app.main` | 运行售后系统 |
| `python -m pytest -q` | 运行自动测试，并简洁显示结果 |
| `git switch -c feature/llm-classifier` | 创建并切换到大模型分类功能分支 |
| `git branch` | 查看本地分支和当前所在分支 |

## Docker Compose

| 命令 | 作用 |
|---|---|
| `docker compose up -d` | 根据 `compose.yaml` 创建并在后台启动服务 |
| `docker compose ps` | 查看当前项目中各个容器的运行和健康状态 |
| `docker compose exec postgres psql -U agent_user -d ecommerce_agent` | 在 PostgreSQL 容器中启动 `psql`，连接指定数据库 |

## PostgreSQL

进入 `psql` 后执行：

| 命令 | 作用 |
|---|---|
| `SELECT version();` | 查看 PostgreSQL 版本 |
| `SELECT current_database(), current_user;` | 查看当前数据库和登录用户 |
| `\q` | 退出 `psql`，返回 PowerShell |
