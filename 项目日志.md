## 2026/9/24

- 基于 LangGraph 构建电商售后工单快速原型，实现问题分类、订单查询、方案生成等任务流。
- 将关键词的规则匹配升级为大模型API语义分类，可识别口语化问题。
- 使用 JSON 结构化输出和 Pydantic 校验分类结果，支持物流、退款、退换货和未知问题。
- 增加 API 超时、自动重试和异常兜底，分类失败时自动转人工服务。
- 完成 3 个 Mock 单元测试和 1 个真实 API 集成测试，全部通过。
- 使用独立配置文件保存 API Key，并通过 `.gitignore` 阻止敏感信息上传。

状态流：START → classify_issue → load_order → generate_resolution → END

## 2026/9/25

- 使用 Docker Compose 部署 PostgreSQL 17，并通过 SQLAlchemy 和 psycopg 完成数据库连接。
- 使用 Alembic管理数据库迁移，创建售后工单表及订单索引。
- 将 LangGraph 工作流接入工单服务，实现真实工单写入和工单编号返回。
- 增加应用层查询与数据库部分唯一索引，实现待处理工单幂等控制。
- 完成工作流单元测试、数据库集成测试和容器重启持久化验证。

## 2026/9/26

- 将订单查询、物流查询和催办工单创建封装为 LangChain Tools，使用 LangGraph 构建可自主选择工具的售后 Agent。
- 在工单工具内部加入订单、物流状态校验和幂等控制，防止模型绕过业务规则创建无效或重复工单。
- 实现渐进式工具披露，按“订单验证 → 物流验证 → 工单执行”逐阶段开放工具，缩小模型每一步的工具选择范围。
- 完善 Agent 评测 Harness，覆盖正常请求、缺失参数、无关问题、提示词注入和强制违规调用等场景，并校验工具名称、参数、调用顺序、执行结果与最终回复。
- 将评测集扩展到 100 条，支持并发执行、结果持久化、离线重新评分，并统计成功率、P50/P95 延迟和吞吐量。
- 对比三种方案：基础全工具 Agent 成功率 97%，长提示词版本下降至 89%，渐进式工具披露提升至 100%，且未出现违规工具调用或违规执行。
- 记录精度与性能权衡：渐进式工具披露提高了工具调用可靠性，但平均响应耗时由 1.904 秒增加至 2.300 秒。

状态流：START → order_agent → order_tools → logistics_agent → logistics_tools → ticket_agent → ticket_tools → respond → END

## 2026/9/27

- 为 LangGraph 工作流接入 `InMemorySaver`，通过 `thread_id` 实现同一会话的上下文延续和不同会话之间的记忆隔离。
- 增加工单状态查询工具，使 Agent 能从历史消息中识别工单号，并从 PostgreSQL 查询最新状态，避免仅根据历史结果作答。
- 引入官方 `langgraph-checkpoint-postgres`，初始化 Checkpoint 表，将消息历史和图执行状态持久化到 PostgreSQL。
- 实现 PostgreSQL 持久化记忆 Agent，在 Python 进程和数据库连接关闭后，仍可根据相同 `thread_id` 恢复会话。
- 增加工具单元测试、短期记忆集成测试和持久化记忆集成测试，并在测试结束后自动清理 Checkpoint 测试数据。
- 增加可重复执行的 Checkpointer 初始化脚本，区分业务工单表与 LangGraph 状态表的管理职责。

记忆链路：thread_id → PostgresSaver → Checkpoint 状态恢复 → 历史工单识别 → get_ticket_status_tool → PostgreSQL 工单查询 → 最新状态回复
